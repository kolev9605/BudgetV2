const initialState = {
    categories: []
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        case 'ACTION':
        default:
            return state
    }
}

export default reducer;